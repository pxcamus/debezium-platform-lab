apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cdc-dashboard.fullname" . }}
type: Opaque
stringData:
  MONGODB_URI: {{ .Values.secret.mongodbUri | quote }}
  POSTGRES_URI: {{ .Values.secret.postgresUri | quote }}